import { globalMutations } from '../mutations/globalData'
import { globalActions } from '../actions/globalData'

const globalData = {
    state: {

    },
    mutations: globalMutations,
    actions: globalActions
}

export default globalData
